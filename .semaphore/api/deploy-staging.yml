---
version: "v1.0"
name: "Api - Deploy Staging"
agent:
  machine:
    type: "e1-standard-2"
    os_image: "ubuntu2004"

blocks:
  - name: "Prepare"
    task:
      jobs:
        - name: "Check asset exists"
          commands:
            - "true"

  - name: "Deploy"
    task:
      jobs:
        - name: "Deploy"
          commands:
            - "true"
